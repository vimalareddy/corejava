package collections;

public class CollectionTest {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		int x = 10, y = 20;
		int[] z = new int[10];
		for (int i = 0; i < z.length; i++) {
			z[i] = i;
		}
		int[] emps = new int[50];
		// 50
		emps[10] = 20;
		emps[11] = 10000;
		emps[12] = 10;

		// sort the emp objs in the ascending of last name

		// collection is a (interface it content most)--> object that represents
		// the group of objects

		// growable in nature
		// collection - store both homogeneous & heterogeneous data types
		// java collections Framework --it provides set of interfaces, abstract
		// implementation of those interfaces, algorithms and implemented
		// collections--> static-- provides set of utility methods to manipulate
		// the collection objects

	}
}
